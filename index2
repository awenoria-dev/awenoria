<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Accueil – Awenoria</title>
  <meta name="description" content="Awenoria – conformité RGPD &amp; IA rendue simple, humaine et accessible. Audits automatisés, accompagnement expert, formations. Scan URL gratuit." />

  <!-- Favicon / PWA -->
  <link rel="icon" type="image/png" sizes="96x96" href="./favicon-96x96.png?v=2">
  <link rel="icon" type="image/svg+xml" href="./images/favicon.svg?v=2">
  <link rel="shortcut icon" href="./favicon.ico?v=2">
  <link rel="apple-touch-icon" sizes="180x180" href="./apple-touch-icon.png?v=2">
  <meta name="apple-mobile-web-app-title" content="Awenoria" />
  <link rel="manifest" href="./site.webmanifest?v=2">
  <meta name="theme-color" content="#0f5f9a">

  <!-- Google Fonts: Manrope (300–800) -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Manrope:opsz,wght@12..96,300..800&display=swap" rel="stylesheet">

  <!-- Styles de la page (inchangés) -->
  <style>
    /* ================== BASE ================== */
    @font-face{
      font-family: "AwenoriaUI";
      src: url("fonts/awenoria-ui.woff2") format("woff2");
      font-weight: 100 900;
      font-display: swap;
    }

    :root{
      --font-ui: "Manrope", system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
      /* Palette bleu (logo) + cuivre */
      --bg:#f6f9fc;
      --panel:#ffffff;
      --muted:#4a6176;
      --text:#0f5f9a;
      --brand:#0f5f9a;
      --accent:#16a34a;
      --copper:#ff8a3d;          /* cuivre */
      --copper-rgb:255,138,61;
      --light-blue:#7bb8ff;      /* bleu clair pour “Client” */
      --danger:#dc2626;
      --radius:16px;
      --shadow:0 12px 28px rgba(0,0,0,.08);
      --maxw:1200px; --nav-h:40px; --caret:.6s;
    }

    *{box-sizing:border-box}
    html,body{height:100%}

    html{
      font-family: var(--font-ui);
      font-weight: 400;
      font-optical-sizing: auto;
      font-synthesis-weight: none;
      font-synthesis-style: none;
    }
    body, button, input, select, textarea { font-family: inherit; }

    body{
      margin:0; color:var(--text);
      background: radial-gradient(1400px 700px at 10% -20%, #eef6ff 0%, rgba(238,246,255,0.6) 45%, transparent 60%),
                  linear-gradient(180deg,#ffffff 0%, #f6f9fc 100%);
      -webkit-font-smoothing:antialiased; -moz-osx-font-smoothing:grayscale; line-height:1.5;
    }

    h1,h2,h3,h4{ font-weight:800; }

    .glass{background:rgba(255,255,255,.86); backdrop-filter: blur(10px); -webkit-backdrop-filter: blur(10px); border:1px solid rgba(15,23,32,.08); box-shadow: var(--shadow)}
    .glass-soft{background:rgba(255,255,255,.78); backdrop-filter: blur(8px); -webkit-backdrop-filter: blur(8px); border:1px solid rgba(15,23,32,.09)}

    /* ================== CORRECTION DES INPUTS ================== */
    input:not([type="checkbox"]):not([type="radio"]),
    select,
    textarea{
      appearance:none;
      -webkit-appearance:none;
      border:1px solid rgba(15,23,32,.18);
      border-radius:12px;
      padding:10px 12px;
      font:inherit;
    }

    /* ================== TITRES ET INTRO ================== */
    h1, h2, h3, h4,
    .title-center,
    .section h2,
    .hero-intro h1 {
      font-family: "Manrope", system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
      font-weight: 800;
      letter-spacing: 0.01em;
    }

    /* ================== HERO ================== */
    .hero-intro{
      position:relative; isolation:isolate;
      padding:64px 0 34px; border-bottom:1px solid rgba(15,23,32,.08);
      background:linear-gradient(180deg, rgba(27,136,255,.06), transparent 55%);
    }
    .hero-intro .wrap{max-width:1280px;margin:0 auto;padding:0 20px;display:grid;grid-template-columns:1.1fr .9fr;gap:36px;align-items:center}
    @media (max-width:980px){.hero-intro .wrap{grid-template-columns:1fr}}
    .hero-intro h1{margin:0 0 12px;font-size:clamp(24px,4.8vw,46px);line-height:1.1}
    .lead{margin:0 0 16px;color:var(--muted);font-size:clamp(16px,1.6vw,18px)}
    .checks{list-style:none;padding:0;margin:0 0 20px;display:grid;gap:10px}
    .checks-inline{display:flex;align-items:center;gap:18px;flex-wrap:wrap}
    .checks li{display:grid;grid-template-columns:22px 1fr;gap:10px;align-items:center}
    .checks li svg{width:20px;height:20px;stroke:var(--accent);fill:none;stroke-width:3;margin-top:0}
    .hero-intro .cta-row{display:flex;flex-wrap:wrap;gap:12px;margin-top:6px;align-items:center}

    .hero-visual{position:relative;border-radius:22px;overflow:hidden;background:var(--panel);border:1px solid rgba(15,23,32,.08);box-shadow:var(--shadow);min-height:280px}
    .hero-visual img{width:100%;height:100%;object-fit:cover;display:block;mix-blend:normal}

    /* ===== URL search pill ===== */
    .url-bar{display:flex;align-items:center;gap:10px;height:56px;padding:8px 8px 8px 14px;background:linear-gradient(180deg,#eef5ff,#edf2f8);border:1.8px solid #b9c8d9;border-radius:999px;box-shadow:inset 0 1px 0 rgba(255,255,255,.85),0 8px 20px rgba(15,23,32,.08)}
    .url-bar input{
      appearance:none;border:none;outline:none;background:transparent;color:var(--text);height:100%;padding:0 10px;
      font-weight:800;letter-spacing:.01em;flex:1;width:auto;
      caret-color: currentColor;
    }
    .url-bar input::placeholder{color:#7aa9c7;font-weight:800;opacity:1}
    .url-bar button{margin-left:auto;height:40px;padding:0 22px;border-radius:999px;border:1px solid rgba(255,255,255,.55);background:linear-gradient(90deg,var(--copper),#ff9a56);color:#fff;font-weight:800;cursor:pointer;box-shadow:inset 0 1px 0 rgba(255,255,255,.75),0 8px 22px rgba(var(--copper-rgb),.35);transition:transform .08s ease,box-shadow .15s ease,filter .15s ease}
    .url-bar button:hover{filter:saturate(1.05) brightness(1.03)}
    .url-bar button:active{transform:translateY(1px)}
    .url-bar button:focus-visible{outline:0;box-shadow:0 0 0 4px rgba(var(--copper-rgb),.25),inset 0 1px 0 rgba(255,255,255,.9)}

    /* ================== SECTIONS ================== */
    .section{max-width:var(--maxw);margin:42px auto;padding:0 20px}
    .title-center{margin:6px 0 16px;text-align:center;font-size:clamp(26px,4vw,40px);font-weight:800}
    .subtitle-center{max-width:860px;margin:0 auto 18px;text-align:center;color:var(--muted)}
    .underline{width:80px;height:4px;border-radius:999px;background:var(--copper);margin:10px auto 26px;opacity:.90;}

    .grid-3{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:18px}
    @media (max-width:1024px){ .grid-3{grid-template-columns:1fr 1fr} }
    @media (max-width:620px){ .grid-3{grid-template-columns:1fr} }

    .card{position:relative;border-radius:22px;padding:22px;display:flex;flex-direction:column;gap:10px;min-height:160px}
    .card{border:1px solid rgba(15,23,32,.08)}
    .badge{align-self:flex-start;border-radius:999px;padding:6px 14px;font-weight:800;background:rgba(27,136,255,.10);border:1px solid rgba(27,136,255,.26);color:#0f3a6e;letter-spacing:.02em;font-size:12px}

    /* Offres/Pricing (structure existante) */
    .pricing{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:18px}
    @media (max-width:980px){.pricing{grid-template-columns:1fr 1fr}}
    @media (max-width:620px){.pricing{grid-template-columns:1fr}}
    .offer{border-radius:22px;border:1px solid rgba(15,23,32,.10);padding:20px;display:grid;gap:12px;background:rgba(255,255,255,.86);backdrop-filter:blur(10px);-webkit-backdrop-filter:blur(10px);box-shadow:var(--shadow)}
    .offer h3{margin:0}
    .offer .for{color:var(--muted)}
    .offer .price{ font-size:28px; font-weight:800; color:var(--copper); text-shadow:0 1px 0 rgba(255,255,255,.6); }

    /* Outil gratuit */
    .auditsamples{display:grid;gap:8px;margin-top:12px}
    .auditsamples .ok,.auditsamples .warn,.auditsamples .bad{display:flex;align-items:center;gap:10px}
    .auditsamples svg{width:18px;height:18px;stroke-width:2;fill:none}
    .ok svg{stroke:var(--accent)}
    .warn svg{stroke:#f59e0b}
    .bad svg{stroke:var(--danger)}

    /* Clients */
    .logos{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:16px;align-items:center}
    @media (max-width:980px){.logos{grid-template-columns:repeat(3,1fr)}}
    @media (max-width:520px){.logos{grid-template-columns:repeat(2,1fr)}}
    .logos .logo{height:44px;border-radius:12px;background:#eef3f9;display:grid;place-items:center;border:1px solid rgba(15,23,32,.06)}

    /* Blog */
    .posts{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:18px}
    @media (max-width:980px){.posts{grid-template-columns:1fr 1fr}}
    @media (max-width:620px){.posts{grid-template-columns:1fr}}
    .post{border-radius:22px;border:1px solid rgba(15,23,32,.10);overflow:hidden}
    .post img{width:100%;height:180px;object-fit:cover;display:block}
    .post .content{padding:16px}

    /* Barre d'écriture (caret) pour textes tapés à l'écran */
    .type-on-view{position:relative;display:inline-block}
    .type-on-view::after{content:"";display:inline-block;width:2px;height:1em;background:currentColor;margin-left:2px;animation:caret-blink var(--caret) steps(1,end) infinite;vertical-align:-0.12em}
    @keyframes caret-blink{50%{opacity:0}}

    /* Contact (styles existants abrégés) */
    .contact-grid{display:grid;grid-template-columns:1.1fr .9fr;gap:18px}
    @media (max-width:980px){.contact-grid{grid-template-columns:1fr}}
    form.contact{display:grid;gap:12px}
    .row{display:grid;grid-template-columns:1fr 1fr;gap:12px}
    @media (max-width:620px){.row{grid-template-columns:1fr}}
    input,select,textarea{appearance:none;border:1px solid rgba(15,23,32,.18);border-radius:12px;padding:10px 12px}
    textarea{min-height:120px}

    .card, .offer, .post, .contact-card, .pill, .hero-visual{background:rgba(255,255,255,.86);backdrop-filter:blur(10px);-webkit-backdrop-filter:blur(10px)}
    .post, .offer, .card{box-shadow: var(--shadow)}

    /* ======= AJOUT: Effet hover sur les 3 offres (seule modif demandée) ======= */
    .offer{ position:relative; transition: transform .22s ease, box-shadow .22s ease, border-color .22s ease, background .22s ease; }
    .offer::before{
      content:""; position:absolute; inset:-10px; border-radius:28px;
      box-shadow: 0 20px 50px rgba(255,138,61,.18);
      opacity:0; transition:opacity .22s ease; pointer-events:none;
    }
    .offer .flag{
      position:absolute; top:-12px; left:20px;
      padding:6px 10px; border-radius:999px;
      font:800 12px/1 var(--font-ui);
      color:#fff; background:linear-gradient(90deg,#ff8a3d,#ff9a56);
      border:1px solid rgba(255,255,255,.66);
      box-shadow:0 8px 16px rgba(255,138,61,.25), inset 0 1px 0 rgba(255,255,255,.9);
      opacity:0; transform:translateY(-6px) scale(.98);
      transition:.22s ease; pointer-events:none;
    }
    .offer .choose{
      display:none; align-self:flex-start; margin-top:6px; padding:10px 14px;
      border-radius:9999px; font-weight:800; font-size:14px; text-decoration:none; color:#fff;
      background:linear-gradient(90deg,var(--copper),#ff9a56);
      box-shadow:0 10px 20px rgba(var(--copper-rgb), .26), inset 0 1px 0 rgba(255,255,255,.9);
      border:1px solid rgba(255,255,255,.55);
      transition:transform .12s ease, filter .18s ease;
    }
    .offer .choose:hover{ filter:brightness(1.03) saturate(1.03); transform:translateY(-1px); }

    .offer:hover{
      transform:translateY(-6px);
      border-color:var(--copper);
      box-shadow:0 16px 36px rgba(0,0,0,.08), 0 0 0 1.5px rgba(255,138,61,.35) inset;
      background:linear-gradient(180deg,#fff,#fff9f5);
    }
    .offer:hover::before{ opacity:1; }
    .offer:hover .flag{ opacity:1; transform:none; }
    .offer:hover .choose{ display:inline-flex; }

    /* focus clavier = même effet que hover */
    .offer:focus-within{
      transform:translateY(-6px);
      border-color:var(--copper);
      box-shadow:0 16px 36px rgba(0,0,0,.08), 0 0 0 1.5px rgba(255,138,61,.35) inset;
      background:linear-gradient(180deg,#fff,#fff9f5);
    }
    .offer:focus-within::before{ opacity:1; }
    .offer:focus-within .flag{ opacity:1; transform:none; }
    .offer:focus-within .choose{ display:inline-flex; }
  </style>
</head>
<body>

  <!-- ===== Header injecté ===== -->
  <div id="header"></div>

  <!-- ============== HERO ============== -->
  <section class="hero-intro" id="intro">
    <div class="wrap">
      <div>
        <h1 class="type-on-view" aria-live="polite" data-text="La conformité RGPD &amp; IA rendue simple, humaine et accessible."></h1>
        <p class="lead">Awenoria vous accompagne vers la conformité numérique grâce à des outils d’audit automatisés et un accompagnement humain personnalisé.</p>

        <ul class="checks checks-inline">
          <li><svg viewBox="0 0 24 24"><path d="M20 6L9 17l-5-5"/></svg>Analyse locale par défaut, sans stockage.</li>
          <li><svg viewBox="0 0 24 24"><path d="M20 6L9 17l-5-5"/></svg>Score global &amp; recommandations priorisées.</li>
          <li><svg viewBox="0 0 24 24"><path d="M20 6L9 17l-5-5"/></svg>RGPD, accessibilité, mentions légales &amp; cookies.</li>
        </ul>

        <div class="cta-row">
          <a class="btn primary" href="#audit-gratuit">Lancer un audit gratuit</a>
          <a class="btn secondary" href="#offres">Voir le détail des packs</a>
        </div>
      </div>
    </div>
  </section>

  <!-- ============== OUTIL GRATUIT D’AUDIT ============== -->
  <section id="audit-gratuit" class="section">
    <h2 class="title-center">Scannez votre site gratuitement</h2>
    <div class="underline" aria-hidden="true"></div>
    <p class="subtitle-center">Entrez simplement l’adresse de votre site pour obtenir un aperçu immédiat de votre conformité RGPD.</p>
    <form class="url-bar" action="#analyse" method="get" onsubmit="event.preventDefault();demoAudit();">
      <input type="url" name="site" id="auditUrl" placeholder="https://votresite.fr" aria-label="Adresse du site à analyser" required autocomplete="off">
      <button type="submit">Analyser votre site</button>
    </form>
    <div class="grid-3" style="margin-top:18px">
      <article class="card glass">
        <h3>Résultats rapides (exemple)</h3>
        <div class="auditsamples">
          <div class="ok"><svg viewBox="0 0 24 24"><path d="M20 6L9 17l-5-5"/></svg> Cookies bloqués avant consentement</div>
          <div class="warn"><svg viewBox="0 0 24 24"><path d="M12 9v4m0 4h.01M12 3a9 9 0 100 18 9 9 0 000-18z"/></svg> Politique de confidentialité absente</div>
          <div class="ok"><svg viewBox="0 0 24 24"><path d="M20 6L9 17l-5-5"/></svg> HTTPS sécurisé</div>
          <div class="bad"><svg viewBox="0 0 24 24"><path d="M6 18L18 6M6 6l12 12"/></svg> Mentions légales non détectées</div>
        </div>
        <div class="cta-row" style="margin-top:12px">
          <a class="btn secondary" href="#">Télécharger le rapport complet (PDF) – 79 €</a>
        </div>
      </article>
      <article class="card glass">
        <span class="badge">Couverture</span>
        <h3>Multi-normes</h3>
        <p>Un seul outil pour vérifier l’essentiel : RGPD, accessibilité, mentions légales et cookies.</p>
      </article>
      <article class="card glass">
        <span class="badge">Impact</span>
        <h3>Diagnostic priorisé</h3>
        <p>Score global + recommandations classées par impact, avec exemples concrets.</p>
      </article>
    </div>
  </section>

  <!-- ============== NOTRE MISSION ============== -->
  <section id="qui-sommes-nous" class="section">
    <h2 class="title-center">Chez Awenoria, la transparence et la confiance sont au cœur de nos engagements.</h2>
    <div class="underline" aria-hidden="true"></div>
    <p class="subtitle-center">
      Cet espace est dédié à une conformité claire et pragmatique. 
      Nous vous aidons à comprendre, sécuriser et valoriser vos données 
      tout en garantissant la conformité de vos activités numériques.
    </p>
    <p class="subtitle-center">
      <a class="btn secondary" href="#approche">Découvrir notre approche</a>
    </p>
  </section>

  <!-- ============== EXPERTISES ============== -->
  <section id="expertises" class="section">
    <h2 class="title-center">Nos domaines d’expertise</h2>
    <div class="underline" aria-hidden="true"></div>
    <div class="grid-3">
      <article class="card expertise glass" id="audit">
        <span class="badge">Audit &amp; Analyse</span>
        <h3>Audit &amp; Analyse automatisée</h3>
        <p>Scannez votre site, vos documents ou vos applications pour identifier les points de non-conformité en quelques minutes.</p>
        <a class="btn primary cta-mini" href="#audit-gratuit">Tester gratuitement</a>
      </article>
      <article class="card expertise glass" id="conformite">
        <span class="badge">Accompagnement</span>
        <h3>Accompagnement &amp; Conformité</h3>
        <p>Un expert vous guide pour structurer vos documents, corriger vos pratiques et maintenir la conformité dans la durée.</p>
        <a class="btn secondary cta-mini" href="#offres">Découvrir nos packs</a>
      </article>
      <article class="card expertise glass" id="formations">
        <span class="badge">Formation</span>
        <h3>Formation &amp; Sensibilisation</h3>
        <p>Des modules concrets pour former vos équipes et comprendre les enjeux RGPD, IA Act et cybersécurité.</p>
        <a class="btn secondary cta-mini" href="#contact">Explorer les formations</a>
      </article>
    </div>
  </section>

  <!-- ============== OFFRES PHARES (avec effet hover) ============== -->
  <section id="offres" class="section">
    <h2 class="title-center">Des solutions adaptées à chaque structure</h2>
    <div class="underline" aria-hidden="true"></div>
    <p class="subtitle-center">Des services flexibles, automatisés ou accompagnés, selon vos besoins et votre budget.</p>

    <div class="pricing" aria-label="Offres">
      <!-- Start RGPD -->
      <article class="offer glass">
        <span class="flag" aria-hidden="true">Populaire</span>
        <h3>Start RGPD</h3>
        <p class="for">TPE &amp; indépendants</p>
        <ul>
          <li>Audit de base</li>
          <li>Politique automatique</li>
          <li>Registre checker</li>
        </ul>
        <p class="price">490 €</p>
        <a href="contact.html?pack=start" class="choose">Choisir cette offre</a>
      </article>

      <!-- Pro RGPD -->
      <article class="offer glass">
        <span class="flag" aria-hidden="true">Populaire</span>
        <h3>Pro RGPD</h3>
        <p class="for">PME / associations</p>
        <ul>
          <li>Audit complet</li>
          <li>PIA / AIPD</li>
          <li>Suivi 6 mois</li>
        </ul>
        <p class="price">1 290 €</p>
        <a href="contact.html?pack=pro" class="choose">Choisir cette offre</a>
      </article>

      <!-- IA Act Starter -->
      <article class="offer glass" id="ia-act">
        <span class="flag" aria-hidden="true">Populaire</span>
        <h3>IA Act Starter</h3>
        <p class="for">Startups IA</p>
        <ul>
          <li>Audit conformité IA</li>
          <li>Documentation exigée</li>
          <li>Feuille de route</li>
        </ul>
        <p class="price">990 €</p>
        <a href="contact.html?pack=iaact" class="choose">Choisir cette offre</a>
      </article>
    </div>

    <p class="subtitle-center" style="margin-top:14px;display:flex;gap:10px;justify-content:center;flex-wrap:wrap">
      <a class="btn secondary" href="#services">Voir le détail des packs</a>
      <a class="btn primary" href="#contact">Obtenir un devis rapide</a>
    </p>
  </section>

  <!-- ============== POURQUOI NOUS CHOISIR ============== -->
  <section id="pourquoi" class="section">
    <h2 class="title-center">Une approche différente de la conformité</h2>
    <div class="underline" aria-hidden="true"></div>
    <div class="grid-3">
      <article class="card glass"><span class="badge">Expertise</span><h3>Humaine</h3><p>Des spécialistes à votre écoute, sans jargon inutile.</p></article>
      <article class="card glass"><span class="badge">Automatisation</span><h3>Intelligente</h3><p>Nos outils analysent ; vous décidez en connaissance de cause.</p></article>
      <article class="card glass"><span class="badge">Transparence</span><h3>Totale</h3><p>Tarifs clairs, méthodes publiées, résultats compréhensibles.</p></article>
    </div>
    <div class="grid-3" style="margin-top:18px">
      <article class="card glass"><span class="badge">Sécurité</span><h3>Confidentialité</h3><p>Hébergement souverain, données chiffrées, aucune revente.</p></article>
      <article class="card glass"><h3>En savoir plus</h3><p>Découvrez notre méthode, nos indicateurs et nos engagements.</p><a class="btn ghost" href="#approche">Notre méthode</a></article>
      <article class="card glass"><h3>Cas clients</h3><p>Des résultats tangibles et mesurables.</p><a class="btn ghost" href="#clients">Voir des exemples</a></article>
    </div>
  </section>

  <!-- ============== ILS NOUS FONT CONFIANCE ============== -->
  <section id="clients" class="section">
    <h2 class="title-center">Ils nous font confiance</h2>
    <div class="underline" aria-hidden="true"></div>
    <div class="logos" aria-label="Logos partenaires">
      <div class="logo">Agence Web</div>
      <div class="logo">Cabinet DPO</div>
      <div class="logo">Association</div>
      <div class="logo">Startup IA</div>
      <div class="logo">Collectivité</div>
      <div class="logo">Éditeur SaaS</div>
    </div>
    <p class="subtitle-center" style="margin-top:10px">Déjà choisi par des TPE, développeurs et formateurs soucieux de leur conformité.</p>
  </section>

  <!-- ============== BLOG / ACTUALITÉS ============== -->
  <section id="blog" class="section">
    <h2 class="title-center">Derniers articles du blog</h2>
    <div class="underline" aria-hidden="true"></div>
    <div class="posts">
      <article class="post glass">
        <img src="images/blog-iaact-2025.jpg" alt="IA Act : ce qui change en 2025">
        <div class="content">
          <h3>IA Act : ce qui change en 2025</h3>
          <p>Panorama des exigences clés et comment s’y préparer en pratique.</p>
        </div>
      </article>
      <article class="post glass">
        <img src="images/blog-rgpd-erreurs.jpg" alt="RGPD : les 10 erreurs les plus fréquentes">
        <div class="content">
          <h3>RGPD : les 10 erreurs les plus fréquentes</h3>
          <p>Des pièges classiques aux solutions concrètes.</p>
        </div>
      </article>
      <article class="post glass">
        <img src="images/blog-audit-score.jpg" alt="Audit web : comment lire votre score de conformité ?">
        <div class="content">
          <h3>Audit web : comment lire votre score de conformité ?</h3>
          <p>Découvrez nos critères et nos pondérations.</p>
        </div>
      </article>
    </div>
    <p class="subtitle-center" style="margin-top:12px"><a class="btn secondary" href="#">Voir tous les articles</a></p>
  </section>

  <!-- ============== CONTACT / DEVIS ============== -->
  <section id="contact" class="section">
    <h2 class="title-center">Parlons de votre conformité</h2>
    <div class="underline" aria-hidden="true"></div>
    <p id="hero-lead" class="subtitle-center type-on-view" data-text="Une question, un devis, un partenariat ? Votre message nous parvient via un envoi sécurisé."></p>

    <div class="contact-form-wrap">
      <div class="contact-grid-2">
        <aside class="contact-card contact-info glass">
          <span class="badge" aria-label="Service client"><span>Service client</span></span>
          <ul class="list">
            <li><strong>contact@awenoria.fr</strong></li>
            <li>Réponse sous 24–48 h ouvrées</li>
            <li>Données traitées en France, RGPD</li>
          </ul>
        </aside>

        <div class="contact-card glass">
          <form id="contactForm" action="https://awenoria.bmavcontact.workers.dev/contact" method="POST" novalidate>
            <div class="form-grid">
              <div>
                <label class="req">Votre demande</label>
                <select name="demande" class="input" id="demande" required>
                  <option value="">Choisissez un élément dans la liste</option>
                  <option value="rdv">Demande de rendez-vous</option>
                  <option value="devis">Demande de devis</option>
                  <option value="partenariat">Partenariat</option>
                  <option value="autre">Autre</option>
                </select>
              </div>
              <div>
                <label class="req">Choisissez le destinataire</label>
                <select name="destinataire" class="input" required>
                  <option value="">Choisissez un élément dans la liste</option>
                  <option>RGPD</option>
                  <option>Hébergement</option>
                  <option>Logiciels et outils</option>
                  <option>Écoconception</option>
                  <option>Accessibilité</option>
                  <option>Autre</option>
                </select>
              </div>
              <div>
                <label class="req">Votre adresse e-mail</label>
                <input class="input" type="email" name="email" placeholder="du type nom@exemple.com" required>
              </div>
              <div style="grid-column:1/-1">
                <label class="req">Votre message</label>
                <textarea class="input" name="message" rows="7" placeholder="Votre message…" required></textarea>
              </div>
            </div>

            <div class="actions" style="display:flex;gap:12px;justify-content:flex-end;margin-top:14px">
              <button id="sendBtn" class="btn primary" type="submit"><span>Envoyer</span></button>
            </div>

            <div class="status-wrap" aria-live="polite" style="margin-top:12px">
              <div id="msgOk" class="status ok" role="status" style="display:none">Merci, votre message a bien été envoyé. Nous revenons vers vous sous 24–48 h ouvrées.</div>
              <div id="msgKo" class="status ko" role="alert" style="display:none">L’envoi a échoué. Réessayez ou écrivez-nous : contact@awenoria.fr.</div>
            </div>
          </form>
        </div>

      </div>
    </div>
  </section>

  <!-- ===== Footer injecté ===== -->
  <div id="footer"></div>

  <!-- ============== SCRIPTS ============== -->

  <!-- Loader header/footer + nav depuis /assets (fourni) -->
  <script src="assets/includes.js" defer></script>

  <!-- Démo d’audit + effet “machine à écrire” (inchangés) -->
  <script>
    const qs=(s,el=document)=>el.querySelector(s);

    function demoAudit(){
      const url = qs('#auditUrl').value || '';
      if(!url) return;
      alert(`Aperçu généré pour: ${url}\n\n• Cookies: OK\n• Politique: À compléter\n• HTTPS: OK\n• Mentions: Manquantes`);
    }

    // Typing-on-view (titres avec caret)
    (function(){
      const SPEED = 85;
      const els = [...document.querySelectorAll('.type-on-view')];
      els.forEach(el=>{
        const full = (el.getAttribute('data-text') || el.textContent || '').trim();
        el.dataset.fullText = full; el.textContent=''; el.__typingTimer=null; el.__ready=true;
        el.setAttribute('aria-live','polite');
      });
      function type(el){
        clearTimeout(el.__typingTimer);
        const full = el.dataset.fullText || ''; el.textContent=''; let i=0;
        (function tick(){ el.textContent = full.slice(0, ++i); if(i<full.length){ el.__typingTimer=setTimeout(tick, SPEED);} else { el.__typingTimer=null; el.__ready=false; } })();
      }
      const io = new IntersectionObserver(entries=>{
        entries.forEach(entry=>{
          const el = entry.target;
          if(entry.isIntersecting){ if(el.__ready) type(el); }
          else{ el.__ready=true; clearTimeout(el.__typingTimer); el.__typingTimer=null; }
        });
      }, { threshold: .4, rootMargin: '0px 0px -10% 0px' });
      els.forEach(el=>io.observe(el));
    })();

    // Envoi simple du formulaire (inchangé)
    (function(){
      const form=document.getElementById('contactForm'); if(!form) return;
      const btn=document.getElementById('sendBtn');
      const okBox=document.getElementById('msgOk');
      const koBox=document.getElementById('msgKo');
      const show=el=>{el.style.display='block'}; const hide=el=>{el.style.display='none'};
      form.addEventListener('submit', async (e)=>{
        e.preventDefault(); if(!form.reportValidity()) return; hide(okBox); hide(koBox);
        btn.disabled=true;
        try{
          const r=await fetch(form.action,{method:'POST', body:new FormData(form), headers:{'Accept':'application/json'}});
          const j=await r.json().catch(()=>({}));
          if(r.ok && j.ok){ show(okBox); form.reset(); setTimeout(()=>hide(okBox),7000);} 
          else { show(koBox); setTimeout(()=>hide(koBox),9000);} 
        }catch(err){
          show(koBox); setTimeout(()=>hide(koBox),9000);
        }finally{
          btn.disabled=false;
        }
      });
    })();
  </script>
</body>
</html>
